export enum DataType {
  String = 'string',
  Number = 'number',
  Boolean = 'boolean',
  Null = 'null',
  Unknown = 'unknown',
}

export enum AggregateType {
  Sum = 'sum',
  Avg = 'avg',
  Max = 'max',
  Min = 'min',
  Count = 'count',
}